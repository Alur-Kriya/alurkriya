<div class="absolute inset-0 -z-10">
  <div class="orbit-background relative w-full overflow-hidden">
    <div class="orbit-circle" style="--size: 8px; --duration: 20s; --delay: -5s; top: 10%;"></div>
    <div class="orbit-circle" style="--size: 12px; --duration: 25s; --delay: -2s; top: 30%;"></div>
    <div class="orbit-circle" style="--size: 10px; --duration: 22s; --delay: -12s; top: 40%;"></div>
    <div class="orbit-circle" style="--size: 5px; --duration: 15s; --delay: -10s; top: 55%;"></div>
    <div class="orbit-circle" style="--size: 7px; --duration: 28s; --delay: -1s; top: 80%;"></div>
  </div>
</div>

<style>
  .orbit-background {
    position: relative;
    width: 100%;
    height: 100%;
  }

  .orbit-circle {
    position: absolute;
    left: -20px; /* Start off-screen */
    width: var(--size);
    height: var(--size);
    background: var(--color-steel-blue-500);
    border-radius: 50%;
    box-shadow: 0 0 8px var(--color-steel-blue-400);
    animation: wave var(--duration) linear infinite;
    animation-delay: var(--delay);
    opacity: 0.6;
  }

  @keyframes wave {
    0% {
      transform: translateX(0vw) translateY(0px);
    }
    25% {
      transform: translateX(25vw) translateY(-20px);
    }
    50% {
      transform: translateX(50vw) translateY(0px);
    }
    75% {
      transform: translateX(75vw) translateY(20px);
    }
    100% {
      transform: translateX(100vw) translateY(0px);
    }
  }

  @media (prefers-color-scheme: dark) {
    .orbit-circle {
      background: var(--color-fresh-sky-500);
      box-shadow: 0 0 8px var(--color-fresh-sky-400);
    }
  }
</style>
