<div class="relative hidden h-full md:flex items-center justify-center">
  <div class="server-rack">
    <div class="server" style="--i: 0;">patch_panel</div>
    <div class="server" style="--i: 1;">firewall</div>
    <div class="server" style="--i: 2;">router</div>
    <div class="server" style="--i: 3;">server_1</div>
    <div class="server" style="--i: 4;">server_2</div>
    <div class="server" style="--i: 5;">server_3</div>
  </div>
</div>

<style>
  .server-rack {
    position: relative;
    width: 200px;
    height: 300px;
    margin: auto;
    perspective: 1000px;
    transform: rotateX(0deg) rotateY(0deg);
    transition: transform 0.8s var(--ease-in-out);
  }

  .server-rack:hover {
    transform: rotateX(0deg) rotateY(0deg) scale(2);
  }

  .server {
    position: absolute;
    width: 100%;
    height: 40px;
    background: var(--color-steel-blue-800);
    border: 1px solid var(--color-steel-blue-950);
    border-radius: 4px;
    transform-style: preserve-3d;
    transition: transform 0.5s cubic-bezier(0.68, -0.55, 0.27, 1.55);
    box-shadow: 0 5px 15px rgba(0, 0, 0, 0.3);
    display: flex;
    padding-top: 5px;
    padding-left: 25px;
    box-sizing: border-box;
  }

  .server::before {
    content: '';
    position: absolute;
    top: 50%;
    left: 10px;
    transform: translateY(-50%);
    width: 8px;
    height: 8px;
    background-color: var(--color-yellow-400);
    border-radius: 50%;
    box-shadow: 0 0 5px var(--color-green-500);
    animation: blink 2s infinite;
    animation-delay: calc(var(--i) * 0.2s);
  }

  .server {
    transform: translateY(calc(var(--i) * 50px));
  }

  .server-rack:hover .server {
    transform: translateY(calc(var(--i) * 60px)) translateZ(calc(var(--i) * 10px));
  }

  @keyframes blink {
    50% {
      opacity: 0.3;
    }
  }

  @media (prefers-color-scheme: dark) {
    .server {
      background: var(--color-ink-black-800);
      border-color: var(--color-ink-black-700);
    }
  }
</style>
